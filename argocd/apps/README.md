# Argo CD Apps

This folder is intentionally mostly empty in the bootstrap state.

Add platform apps here when you want Argo CD to manage them after initial bootstrap.

Client apps should be added under:

```text
clients/<client-slug>/
```
